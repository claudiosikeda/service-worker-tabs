{
  "name": "service-worker-tabs",
  "version": "1.0.0",
  "description": "Managing tabs with service worker",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --open public/ --node-resolve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Claudio Ikeda",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.1.31"
  }
}
